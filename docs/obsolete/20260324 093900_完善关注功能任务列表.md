# 完善"关注"功能任务列表

## 项目信息

- **文档生成时间：** 2026-03-24 19:23:00
- **项目名称：** 圆运阁古易文化 - 排盘系统
- **功能模块：** 用户关注功能
- **数据库：** SQLite 3.x（数据库文件：backend/app/db/yyggywh.db）
- **ORM框架：** SQLAlchemy

## 功能概述

完善首页弹窗中的"关注"按钮功能，实现用户之间的关注/取消关注交互，包括完整的后端API、数据库模型、服务层和前端实现。

## 任务列表

### 后端开发任务

#### 1. 创建关注关系数据库模型

**文件位置：** `backend/app/models/user_follow.py`

**任务内容：**
- 定义 `UserFollow` 模型类
- 包含字段：
  - `id`: 主键
  - `follower_id`: 关注者ID（外键关联User表）
  - `followed_id`: 被关注者ID（外键关联User表）
  - `created_at`: 创建时间戳
- 添加数据库索引优化查询性能
- 在 `backend/app/models/__init__.py` 中导出模型

**依赖关系：**
- 需要先完成数据库迁移脚本的创建

---

#### 2. 编写关注功能的服务层业务逻辑

**文件位置：** `backend/app/services/follow_service.py`

**任务内容：**
- 实现 `toggle_follow` 方法：处理关注/取消关注逻辑
- 实现 `is_following` 方法：检查用户是否已关注
- 实现 `get_followers` 方法：获取用户粉丝列表
- 实现 `get_followed` 方法：获取用户关注列表
- 实现 `get_follower_count` 方法：获取粉丝数量
- 实现 `get_followed_count` 方法：获取关注数量
- 添加异常处理和事务管理

**业务规则：**
- 防止用户关注自己
- 防止重复关注
- 取消关注时正确删除记录

---

#### 3. 实现关注/取消关注的API接口

**文件位置：** `backend/app/api/follow.py`

**任务内容：**
- 创建 FastAPI 路由
- 实现 POST `/follow/toggle` 接口：关注/取消关注
- 实现 GET `/follow/status` 接口：获取关注状态
- 实现 GET `/follow/followers` 接口：获取粉丝列表
- 实现 GET `/follow/followed` 接口：获取关注列表
- 添加请求验证（Pydantic模型）
- 添加速率限制
- 添加认证装饰器

**请求示例：**
```json
// 关注/取消关注
POST /api/v1/follow/toggle
{
  "followed_id": 123
}

// 获取关注状态
GET /api/v1/follow/status?followed_id=123
```

**响应示例：**
```json
// 成功响应
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "is_followed": true,
    "follower_count": 10,
    "followed_count": 5
  }
}
```

---

#### 4. 生成数据库迁移脚本

**文件位置：** `backend/migrations/versions/xxx_create_user_follow_table.py`

**任务内容：**
- 使用 Alembic 创建迁移脚本
- 创建 `user_follow` 表
- 添加外键约束
- 添加索引优化查询

**迁移命令：**
```bash
cd backend
alembic revision -m "Create user_follow table"
alembic upgrade head
```

---

### 前端开发任务

#### 5. 在前端API文件中添加关注接口调用方法

**文件位置：** `frontend/src/api/userApi.js`

**任务内容：**
- 添加 `toggleFollow` 函数：调用后端关注/取消关注API
- 添加 `getFollowStatus` 函数：获取关注状态
- 添加 `getFollowers` 函数：获取粉丝列表
- 添加 `getFollowed` 函数：获取关注列表

**代码示例：**
```javascript
export const toggleFollow = async (followedId) => {
  try {
    const response = await api.post('/follow/toggle', {
      followed_id: followedId
    });
    return response;
  } catch (error) {
    throw error;
  }
};
```

---

#### 6. 完善PanDetailModal.jsx中的handleFollow函数

**文件位置：** `frontend/src/components/Modal/PanDetailModal.jsx`

**任务内容：**
- 导入 `toggleFollow` API函数
- 导入 `isFrontendLoggedIn` 工具函数
- 重写 `handleFollow` 函数：
  - 检查用户是否登录
  - 调用后端API
  - 处理成功响应
  - 处理失败情况（回滚UI）
  - 添加加载状态
- 添加错误处理和日志记录

**代码示例：**
```javascript
const handleFollow = async (e) => {
  e.stopPropagation();
  
  // 检查登录状态
  if (!isFrontendLoggedIn()) {
    navigate('/login', { state: { from: window.location.pathname } });
    return;
  }
  
  // 设置加载状态
  setIsLoading(true);
  
  try {
    const response = await toggleFollow(data.user_id);
    if (response.code === 200) {
      setIsFollowed(response.data.is_followed);
      setFollowerCount(response.data.follower_count);
    } else {
      // 失败时回滚
      setIsFollowed(isFollowed);
    }
  } catch (error) {
    console.error('关注失败:', error);
    setIsFollowed(isFollowed);
  } finally {
    setIsLoading(false);
  }
};
```

---

#### 7. 添加用户登录状态检查

**任务内容：**
- 在关注按钮点击前检查用户登录状态
- 未登录用户跳转到登录页面
- 记录当前页面路径，登录后返回

**实现位置：**
- `frontend/src/components/Modal/PanDetailModal.jsx`
- `frontend/src/components/Modal/components/UserInfo/UserInfo.jsx`

---

#### 8. 实现加载状态和错误处理

**任务内容：**
- 为关注按钮添加 `isLoading` 状态
- 显示加载动画或禁用按钮
- 添加错误提示（Toast通知）
- 失败时回滚UI状态

**用户体验优化：**
- 加载时显示"处理中..."
- 成功时显示"关注成功"或"取消关注成功"
- 失败时显示错误信息并允许重试

---

#### 9. 更新UserInfo组件

**文件位置：** `frontend/src/components/Modal/components/UserInfo/UserInfo.jsx`

**任务内容：**
- 添加 `isLoading` prop
- 添加 `followerCount` prop
- 更新按钮样式以支持加载状态
- 优化按钮点击事件处理

---

### 测试任务

#### 10. 添加关注功能的单元测试

**文件位置：** `backend/tests/test_follow_api.py`

**任务内容：**
- 测试关注功能
- 测试取消关注功能
- 测试重复关注处理
- 测试关注自己处理
- 测试获取关注状态
- 测试获取粉丝列表
- 测试获取关注列表
- 测试权限验证

**测试覆盖：**
- 正常场景
- 边界条件
- 异常场景

---

#### 11. 添加前端单元测试

**文件位置：** `frontend/src/components/Modal/PanDetailModal.test.jsx`

**任务内容：**
- 测试关注按钮点击
- 测试登录状态检查
- 测试API调用成功场景
- 测试API调用失败场景
- 测试加载状态显示

---

### 部署任务

#### 12. 数据库迁移部署

**任务内容：**
- 在测试环境执行数据库迁移
- 验证表结构正确性
- 检查索引是否生效
- 备份生产环境数据
- 在生产环境执行迁移

---

#### 13. 性能优化

**任务内容：**
- 检查数据库查询性能
- 优化索引设计
- 添加缓存机制（可选）
- 监控慢查询日志

---

#### 14. 监控和日志

**任务内容：**
- 添加关注操作日志记录
- 监控API调用频率
- 设置异常告警
- 统计关注功能使用情况

---

## 任务执行顺序

### 阶段一：后端基础建设（优先级：高）
1. 创建关注关系数据库模型
2. 生成数据库迁移脚本
3. 编写关注功能的服务层业务逻辑

### 阶段二：后端API开发（优先级：高）
4. 实现关注/取消关注的API接口
5. 在 `backend/app/api/__init__.py` 中注册路由

### 阶段三：前端集成（优先级：高）
6. 在前端API文件中添加关注接口调用方法
7. 完善PanDetailModal.jsx中的handleFollow函数
8. 添加用户登录状态检查
9. 实现加载状态和错误处理

### 阶段四：测试和优化（优先级：中）
10. 添加关注功能的单元测试
11. 添加前端单元测试
12. 性能优化
13. 监控和日志

### 阶段五：部署（优先级：中）
14. 数据库迁移部署
15. 功能上线

---

## 预估完成时间

| 阶段 | 任务 | 预估时间 |
|------|------|----------|
| 后端基础建设 | 1-3 | 2-3小时 |
| 后端API开发 | 4 | 1-2小时 |
| 前端集成 | 5-9 | 2-3小时 |
| 测试和优化 | 10-13 | 2-3小时 |
| 部署 | 14-15 | 1小时 |
| **总计** | | **8-12小时** |

---

## 需要注意的关键点

### 1. 安全性
- 必须实现用户认证验证
- 防止SQL注入
- 防止XSS攻击
- 限制API调用频率

### 2. 数据一致性
- 使用数据库事务确保数据一致性
- 处理并发关注/取消关注情况
- 定期清理无效数据

### 3. 性能优化
- 添加适当的数据库索引
- 考虑使用缓存减少数据库查询
- 优化查询语句避免N+1问题

### 4. 用户体验
- 提供清晰的操作反馈
- 处理各种异常情况
- 保证操作的原子性

### 5. 可维护性
- 编写清晰的代码注释
- 遵循项目代码规范
- 添加完整的单元测试

---

## 相关文件清单

### 后端文件
- `backend/app/models/user_follow.py` (新建)
- `backend/app/services/follow_service.py` (新建)
- `backend/app/api/follow.py` (新建)
- `backend/app/api/__init__.py` (修改)
- `backend/migrations/versions/xxx_create_user_follow_table.py` (新建)
- `backend/tests/test_follow_api.py` (新建)

### 前端文件
- `frontend/src/api/userApi.js` (修改)
- `frontend/src/components/Modal/PanDetailModal.jsx` (修改)
- `frontend/src/components/Modal/components/UserInfo/UserInfo.jsx` (修改)
- `frontend/src/components/Modal/PanDetailModal.test.jsx` (新建)

---

## 成功标准

- [ ] 用户可以成功关注/取消关注其他用户
- [ ] 关注状态在页面刷新后保持正确
- [ ] 未登录用户无法关注，会跳转到登录页面
- [ ] 所有API接口都有完整的错误处理
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] API响应时间 < 200ms
- [ ] 数据库查询有适当的索引优化
- [ ] 所有操作都有日志记录
- [ ] 用户体验流畅，无明显卡顿

---

## 后续优化建议

1. **关注推荐**：根据用户兴趣推荐可能关注的用户
2. **关注分组**：允许用户对关注列表进行分组管理
3. **隐私设置**：允许用户设置关注列表的可见性
4. **消息通知**：被关注时发送通知提醒
5. **数据分析**：统计关注转化率、留存率等指标

---

**文档版本：** v1.0  
**最后更新：** 2026-03-24 19:23:00  
**作者：** AI Assistant
