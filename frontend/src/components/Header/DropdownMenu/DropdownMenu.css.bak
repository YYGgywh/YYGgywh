/* 路径:src/components/Header/DropdownMenu/DropdownMenu.css 时间:2026-02-07 11:00 */
/* 功能:下拉菜单组件样式，包含多列布局和悬停效果 */
.dropdown-menu-rich {
  position: absolute;
  top: 100%;
  left: 0;
  background-color: #FFFFFF;
  border-radius: 8px;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  z-index: 100;
  opacity: 0;
  transform: translateY(5px);
  animation: dropdownFadeIn 0.2s ease forwards;
  min-width: 600px !important;
  width: auto !important;
  padding: 5px;
}

@keyframes dropdownFadeIn {
  from {
    opacity: 0;
    transform: translateY(-5px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.dropdown-menu-content {
  display: flex !important;
  flex-direction: row !important;
  gap: 40px;
  flex-wrap: nowrap !important;
  justify-content: space-between;
  width: 100% !important;
}

.dropdown-menu-content dl {
  margin: 0 !important;
  padding: 0 !important;
  display: flex !important;
  flex-direction: column !important;
  flex: 1 1 0 !important;
  min-width: 100px !important;
  max-width: calc(33.333% - 26.666px) !important;
  box-sizing: border-box !important;
}

.dropdown-menu-content dl dt,
.dropdown-menu-content dl dd {
  margin: 0 !important;
  padding: 0 !important;
}

@media (max-width: 768px) {
  .dropdown-menu-rich {
    min-width: 280px;
    padding: 16px;
  }

  .dropdown-menu-content {
    gap: 24px;
  }
}
