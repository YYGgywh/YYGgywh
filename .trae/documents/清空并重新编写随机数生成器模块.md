1. 清空 `random_generator.py` 文件的所有现有代码
2. 导入必要的模块：`random`、`secrets`、`time` 和类型注解
3. 创建 `RandomGenerator` 类，包含以下静态方法：

   * `uniform_random`: 生成均匀分布随机整数

   * `uniform_float_random`: 生成均匀分布随机浮点数

   * `gaussian_random`: 生成高斯分布随机数

   * `bounded_gaussian_random`: 生成有界高斯分布随机数

   * `secure_random_digit`: 生成安全随机数字字符串

   * `secure_random_hex`: 生成安全随机十六进制字符串

   * `random_jiazi`: 随机选择六十甲子干支

   * `coin_toss`: 模拟硬币投掷

   * `dice_roll`: 模拟骰子投掷

   * `set_seed`: 设置随机数种子

   * `get_current_seed`: 获取当前随机数状态
4. 实现便捷函数，保持与原有API兼容
5. 添加测试函数用于验证功能
6. 确保代码符合Python最佳实践和类型注解要求

